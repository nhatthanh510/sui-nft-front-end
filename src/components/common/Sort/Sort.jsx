import React from 'react';

export default function Sort() {
  return (
    <div className="mt-12 text-right">
      <select className="select w-full max-w-[180px] bg-black">
        <option selected>Price</option>
        <option>Priceeeee</option>
      </select>
    </div>
  );
}
